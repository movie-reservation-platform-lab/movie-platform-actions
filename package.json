{
  "name": "movie-platform-actions",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:generated": "npm run build && git diff --exit-code -- actions/container-evidence/lib",
    "test": "node --test test/*.test.mjs",
    "ci": "npm run check:generated && npm test"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "typescript": "7.0.2"
  }
}
