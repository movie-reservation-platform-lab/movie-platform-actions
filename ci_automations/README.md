# Who watches the watchers?

This repository provides CI building blocks for other repositories. Those
building blocks need quality checks of their own. This folder holds the
automations that check this package itself. CI for the CI, if you will.
